# Qwen2.5-14B LoRA-GRPO 正式训练命令

本文档给出 8.1 主对比实验中 Ours（14B 本地模型 + 完整三类奖励 RL）的正式
训练、监控和断点续训命令。概念解释见
[`docs/训练流程与讲解.md`](训练流程与讲解.md)。

对比组 1 中的 NavGPT GPT-3.5 是零样本基线，不需要训练；下面训练的仅是 Ours。

## 1. 已验证的正式配置

当前服务器与代码已真实通过：

- 4 × NVIDIA RTX PRO 6000 Blackwell Server Edition；
- Qwen2.5-14B BF16 + LoRA；
- 4-GPU DDP、同任务 4 rollout、LoRA-only 梯度同步；
- `blackwell-safe` NCCL transport；
- 连续训练与 checkpoint 恢复逐张量一致；
- Level 2 全量 token budget，当前
  `recommended_max_completion_length=38912`。

正式训练使用 R2R train 的 14,039 条 instruction，运行 2 epochs。默认拓扑下
约有 28,078 个 optimizer step 和 112,312 条 rollout。这个任务可能运行很久，
不能用此前 1/2-step smoke test 的耗时直接估算完整训练耗时。

## 2. 进入容器并做启动前检查

在宿主机执行：

```bash
docker exec -it rl-navgpt bash
```

在容器内执行：

```bash
set -e

cd /workspace/RL-NavGPT/nav_src

TRAIN_PYTHON=/home/xl/venvs/navgpt-rl/bin/python

unset CUDA_VISIBLE_DEVICES
unset WORLD_SIZE RANK LOCAL_RANK MASTER_ADDR MASTER_PORT

PYTHONNOUSERSITE=1 "$TRAIN_PYTHON" - <<'PY'
import torch

print("torch:", torch.__version__)
print("CUDA available:", torch.cuda.is_available())
print("GPU count:", torch.cuda.device_count())
assert torch.cuda.is_available()
assert torch.cuda.device_count() == 4
for index in range(4):
    print(index, torch.cuda.get_device_name(index))
print("PASS: four GPUs are visible")
PY

nvidia-smi
df -h /workspace/RL-NavGPT
```

如果 CUDA 为 `False`，先退出容器并执行 `docker restart rl-navgpt`，不要继续
启动训练。容器重启不是容器重建，不会删除 bind mount 中的项目、数据或 venv。

确认没有遗留训练进程：

```bash
pgrep -af 'train_grpo.py|torch.distributed.run' || true
```

如果这里列出了仍在运行的有效训练，不要再次启动；先确认它属于哪个输出目录。

### 2.1 首次训练前生成 Val-Unseen 的固定 Planner 缓存

训练集 Planner 缓存不能用于 Val-Unseen。以下操作只需成功完成一次；中断后
原命令重跑会从各 shard 已完成的位置继续：

```bash
cd /workspace/RL-NavGPT/nav_src

PLANNER_PYTHON=/home/xl/venvs/qwen-tf448/bin/python
PLANNER_MODEL=../models/Qwen2.5-14B-Instruct-1M
VAL_ANNOTATION=../datasets/R2R/annotations/R2R_val_unseen_instr.json
VAL_PLAN_DIR=../datasets/R2R/action_plan_cache/qwen2.5-14b-val-unseen-t0-v1

mkdir -p "$VAL_PLAN_DIR"

for SHARD_ID in 0 1 2 3; do
  nohup env \
    CUDA_VISIBLE_DEVICES="$SHARD_ID" \
    PYTHONNOUSERSITE=1 \
    PYTHONUNBUFFERED=1 \
    "$PLANNER_PYTHON" scripts/action_planner.py generate \
      --annotation "$VAL_ANNOTATION" \
      --output "$VAL_PLAN_DIR/shard-$SHARD_ID.jsonl" \
      --shard-id "$SHARD_ID" \
      --num-shards 4 \
      --expected-count 2349 \
      --local-model-path "$PLANNER_MODEL" \
      --model-id Qwen/Qwen2.5-14B-Instruct-1M \
      --model-revision modelscope-master-fileset-46627f7a9e85 \
      --expected-transformers-version 4.48.3 \
      --chat-template auto \
      --dtype bf16 \
      --temperature 0 \
      --top-p 0.9 \
      --max-new-tokens 256 \
      --seed 0 \
    > "$VAL_PLAN_DIR/shard-$SHARD_ID.log" 2>&1 &
done
```

监控：

```bash
wc -l "$VAL_PLAN_DIR"/shard-[0-3].jsonl
tail -f "$VAL_PLAN_DIR/shard-0.log"
```

四个 shard 完成数量应为 `588 587 587 587`。全部完成后合并：

```bash
PYTHONNOUSERSITE=1 "$PLANNER_PYTHON" \
  scripts/action_planner.py merge \
    --annotation "$VAL_ANNOTATION" \
    --inputs \
      "$VAL_PLAN_DIR/shard-0.jsonl" \
      "$VAL_PLAN_DIR/shard-1.jsonl" \
      "$VAL_PLAN_DIR/shard-2.jsonl" \
      "$VAL_PLAN_DIR/shard-3.jsonl" \
    --output "$VAL_PLAN_DIR/R2R_val_unseen_action_plans.jsonl" \
    --num-shards 4 \
    --expected-count 2349

test -f "$VAL_PLAN_DIR/R2R_val_unseen_action_plans.jsonl.manifest.json"
wc -l "$VAL_PLAN_DIR/R2R_val_unseen_action_plans.jsonl"
```

最后必须是 2349。正式训练启动时会再次逐条校验 instruction、action plan、
Planner fingerprint 和 manifest；缺一条都会在加载 14B Policy 前停止。

先运行不加载 Qwen/CLIP 的最小契约测试：

```bash
TRAIN_PYTHON=/home/xl/venvs/navgpt-rl/bin/python

PYTHONNOUSERSITE=1 "$TRAIN_PYTHON" \
  scripts/validate_r2r_evaluation.py
```

最后应输出 `PASS R2R validation contract`。固定子集文件一旦创建，annotation、
seed、数量或 ID 有任何变化都会报错，不会静默重选 128 条样本。

## 3. 读取正式 token budget

正式训练必须读取 Level 2 报告，不能使用 Level 3/4 的 smoke budget：

```bash
cd /workspace/RL-NavGPT/nav_src

TRAIN_PYTHON=/home/xl/venvs/navgpt-rl/bin/python
VALIDATION_ROOT=$(cat /tmp/navgpt-stage6-validation-root)
LEVEL2_REPORT=$(realpath "$VALIDATION_ROOT/level2_report.json")

test -f "$LEVEL2_REPORT"

MAX_COMPLETION_LENGTH=$(
  "$TRAIN_PYTHON" -c \
  'import json,sys; print(json.load(open(sys.argv[1]))["token_budget"]["recommended_max_completion_length"])' \
  "$LEVEL2_REPORT"
)

echo "LEVEL2_REPORT=$LEVEL2_REPORT"
echo "MAX_COMPLETION_LENGTH=$MAX_COMPLETION_LENGTH"

test "$MAX_COMPLETION_LENGTH" -gt 0
```

当前代码和数据对应的预期输出是：

```text
MAX_COMPLETION_LENGTH=38912
```

若不是 38912，不要手工覆盖；先确认是否读取了另一轮 Level 2 报告。

### 3.1 最后一次真实四卡闭环验收

上传本批代码后、正式长训练前只运行一次。它使用真实 Qwen、真实 GRPO 和从
Val-Unseen 固定截取的 16 条临时 fixture，但不把 fixture 分数当论文结果。

脚本会自动执行：训练 step 1 → 在 fast job 已持久化后终止 torchrun → 以
`--validation-only` 恢复评测且不训练 → 从 checkpoint-1 恢复到 step 2 → 完成
第二次快评与 epoch-end 完整 fixture 评测 → 检查 quick/full best。

```bash
set -e

cd /workspace/RL-NavGPT/nav_src

TRAIN_PYTHON=/home/xl/venvs/navgpt-rl/bin/python
VALIDATION_ROOT=$(cat /tmp/navgpt-stage6-validation-root)
LEVEL2_REPORT=$(realpath "$VALIDATION_ROOT/level2_report.json")

MAX_COMPLETION_LENGTH=$(
  "$TRAIN_PYTHON" -c \
  'import json,sys; print(json.load(open(sys.argv[1]))["token_budget"]["recommended_max_completion_length"])' \
  "$LEVEL2_REPORT"
)

CYCLE_ROOT="../outputs/grpo-validation-cycle-$(date +%Y%m%d-%H%M%S)"
test ! -e "$CYCLE_ROOT"

PYTHONNOUSERSITE=1 \
PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True \
TRL_EXPERIMENTAL_SILENCE=1 \
TOKENIZERS_PARALLELISM=false \
OMP_NUM_THREADS=1 \
"$TRAIN_PYTHON" scripts/validate_grpo_validation_cycle.py \
  --validation-root "$CYCLE_ROOT" \
  --gpus 0,1,2,3 \
  --nccl-profile blackwell-safe \
  --max-completion-length "$MAX_COMPLETION_LENGTH"
```

最终必须输出：

```text
PASS real four-GPU train/evaluate/resume validation
- training was interrupted with fast-step-1 persisted in the queue
- evaluation-only drained the queue without an optimizer step
- checkpoint-1 resumed through step 2 and epoch-end full selection
```

详细报告为 `$CYCLE_ROOT/report.json`，三个阶段的控制台分别保存在
`phase1-train-interrupted.log`、`phase2-evaluation-only.log` 和
`phase3-training-resumed.log`。第一阶段非零退出是脚本主动制造的测试条件，
不是服务器故障；脚本只终止自己启动的独立进程组，不会停止 Docker 容器。

若 GPU 2/3 正被实验室其他任务占用，可以先在 GPU 0/1 上运行完全相同的恢复
闭环，只把参数改成 `--gpus 0,1`。此时仍使用 `num_generations=4`，每张卡顺序
生成两条候选，最终输出中的名称相应变为：

```text
PASS real 2-GPU train/evaluate/resume validation
```

这能验证 snapshot、评测队列、best selector 和训练恢复逻辑，但不改变正式主
训练仍使用 `--gpus 0,1,2,3` 的配置。

## 4. 正式四卡训练命令

以下命令创建一个全新输出目录并在后台运行。训练中不要修改
`distributed_runtime.py`、`grpo_runtime.py`、`grpo_training.py`、
`lora_policy.py`、`rl_env.py`、奖励/状态/提示词或训练入口；它们已经进入 run
manifest，修改后将无法对原 run 做安全续训。

```bash
set -e

cd /workspace/RL-NavGPT/nav_src

TRAIN_PYTHON=/home/xl/venvs/navgpt-rl/bin/python
VALIDATION_ROOT=$(cat /tmp/navgpt-stage6-validation-root)
LEVEL2_REPORT=$(realpath "$VALIDATION_ROOT/level2_report.json")

MAX_COMPLETION_LENGTH=$(
  "$TRAIN_PYTHON" -c \
  'import json,sys; print(json.load(open(sys.argv[1]))["token_budget"]["recommended_max_completion_length"])' \
  "$LEVEL2_REPORT"
)

RUN_TAG="main8.1-ours-qwen14b-fullrl-seed0-$(date +%Y%m%d-%H%M%S)"
OUTPUT_DIR="/workspace/RL-NavGPT/outputs/$RUN_TAG"
CONSOLE_LOG="${OUTPUT_DIR}.console.log"

test "$MAX_COMPLETION_LENGTH" -gt 0
test ! -e "$OUTPUT_DIR"
test ! -e "$CONSOLE_LOG"

mkdir -p /workspace/RL-NavGPT/outputs

git -C /workspace/RL-NavGPT rev-parse HEAD \
  > "${OUTPUT_DIR}.git-commit.txt"
git -C /workspace/RL-NavGPT status --short \
  > "${OUTPUT_DIR}.git-status.txt"

printf '%s\n' "$OUTPUT_DIR" \
  > /tmp/navgpt-main8.1-output-dir
printf '%s\n' "$CONSOLE_LOG" \
  > /tmp/navgpt-main8.1-console-log
printf '%s\n' "$MAX_COMPLETION_LENGTH" \
  > /tmp/navgpt-main8.1-max-completion-length

nohup env \
  PYTHONNOUSERSITE=1 \
  PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True \
  TRL_EXPERIMENTAL_SILENCE=1 \
  TOKENIZERS_PARALLELISM=false \
  OMP_NUM_THREADS=1 \
  "$TRAIN_PYTHON" scripts/launch_grpo.py \
    --mode ddp \
    --gpus 0,1,2,3 \
    --nccl-profile blackwell-safe \
    -- \
    --output-dir "$OUTPUT_DIR" \
    --expected-instruction-count 14039 \
    --clip-text-device auto \
    --clip-text-dtype fp16 \
    --max-navigation-steps 10 \
    --r 16 \
    --lora-alpha 32 \
    --lora-dropout 0.05 \
    --max-trainable-percentage 1.0 \
    --dtype bf16 \
    --max-completion-length "$MAX_COMPLETION_LENGTH" \
    --num-generations 4 \
    --learning-rate 1e-6 \
    --weight-decay 0.0 \
    --warmup-ratio 0.03 \
    --max-grad-norm 1.0 \
    --lr-scheduler-type cosine \
    --beta 0.001 \
    --temperature 1.0 \
    --top-p 0.95 \
    --max-tool-calling-iterations 10 \
    --trainer-max-steps -1 \
    --num-train-epochs 2.0 \
    --logging-steps 1 \
    --save-steps 50 \
    --save-total-limit 3 \
    --trajectory-log-interval 10 \
    --validation \
    --validation-annotation ../datasets/R2R/annotations/R2R_val_unseen_instr.json \
    --validation-action-plan-cache ../datasets/R2R/action_plan_cache/qwen2.5-14b-val-unseen-t0-v1/R2R_val_unseen_action_plans.jsonl \
    --validation-fast-subset-manifest ../datasets/R2R/eval_subsets/R2R_val_unseen_fast128_seed0.json \
    --validation-expected-instruction-count 2349 \
    --validation-fast-subset-size 128 \
    --validation-fast-interval-steps 1000 \
    --validation-max-new-tokens 512 \
    --validation-seed 0 \
    --seed 0 \
  > "$CONSOLE_LOG" 2>&1 &

TRAIN_PID=$!
printf '%s\n' "$TRAIN_PID" \
  > /tmp/navgpt-main8.1-pid

echo "TRAIN_PID=$TRAIN_PID"
echo "OUTPUT_DIR=$OUTPUT_DIR"
echo "CONSOLE_LOG=$CONSOLE_LOG"
echo "MAX_COMPLETION_LENGTH=$MAX_COMPLETION_LENGTH"
```

命令中故意没有填写以下两个参数：

```text
--steps-per-generation
--gradient-accumulation-steps
```

四卡入口会自动把二者都设置为 1，使每张卡各生成一条 rollout，合起来恰好是
同一任务的 4 条候选。也没有添加 `--full-determinism`，因为它只用于逐字节
恢复验证，正式长训练会受到明显的性能影响。

## 5. 正式命令字段说明

### 5.1 启动层

| 字段 | 含义 |
| --- | --- |
| `--mode ddp` | 使用 `torchrun` 启动多进程 DistributedDataParallel |
| `--gpus 0,1,2,3` | 使用四张物理 GPU，同时决定 `nproc_per_node=4` |
| `--nccl-profile blackwell-safe` | 使用已经在当前 PCIe Blackwell 服务器验证过的安全 NCCL transport |
| `--` | 此前是 launcher 参数，此后全部转交 `train_grpo.py` |

### 5.2 数据与环境

未显式给出的 annotation、action-plan cache、场景文本、connectivity、CLIP
cache、CLIP 模型和 Qwen 模型路径使用 `train_grpo.py` 中已经验证过的项目默认值。

| 字段 | 含义 |
| --- | --- |
| `expected-instruction-count=14039` | 防止误读不完整或错误的 train split |
| `clip-text-device=auto` | 每个 rank 在已经绑定的本地 GPU 上运行冻结 CLIP text tower |
| `clip-text-dtype=fp16` | CLIP 在线思维文本编码采用 FP16；缓存模型身份仍会严格校验 |
| `max-navigation-steps=10` | 每条导航 episode 最多 10 个环境动作 |
| `max-tool-calling-iterations=10` | TRL 最多允许 10 轮工具调用，与环境上限一致 |

### 5.3 LoRA

| 字段 | 含义 |
| --- | --- |
| `r=16` | LoRA 低秩维度 |
| `lora-alpha=32` | LoRA 更新缩放系数 |
| `lora-dropout=0.05` | adapter 配置值；GRPO 中实际 dropout 被统一禁用 |
| `max-trainable-percentage=1.0` | 安全上限：可训练参数不得超过全模型 1%，防止主干意外解冻 |
| `dtype=bf16` | Qwen/LoRA 的训练计算精度 |

### 5.4 GRPO 与优化器

| 字段 | 含义 |
| --- | --- |
| `max-completion-length` | Level 2 审计出的完整多轮 transcript token 上限 |
| `num-generations=4` | 每个 task 采样 4 条 rollout 做组内比较 |
| `learning-rate=1e-6` | LoRA AdamW 学习率 |
| `weight-decay=0` | 不对 LoRA 施加额外权重衰减 |
| `warmup-ratio=0.03` | 前 3% optimizer steps 做学习率 warmup |
| `max-grad-norm=1.0` | 梯度范数超过 1 时裁剪 |
| `lr-scheduler-type=cosine` | warmup 后使用 cosine 学习率衰减 |
| `beta=0.001` | reference policy KL 约束强度 |
| `temperature=1.0` | rollout 采样温度，提供组内探索差异 |
| `top-p=0.95` | nucleus sampling 的累计概率阈值 |

### 5.5 训练长度、日志与保存

| 字段 | 含义 |
| --- | --- |
| `trainer-max-steps=-1` | 不用固定 step 截断，让 Trainer 按 epoch 结束 |
| `num-train-epochs=2.0` | 完整遍历 14,039 个训练任务两次 |
| `logging-steps=1` | 每个 optimizer step 写一次聚合指标 |
| `save-steps=50` | 每 50 个 optimizer steps 保存一次完整 checkpoint |
| `save-total-limit=3` | 只保留最近 3 个 checkpoint |
| `trajectory-log-interval=10` | 每 10 个 group 额外记录一次详细逐步轨迹 |
| `seed=0` | 当前首个正式训练随机种子 |

### 5.6 验证与 best 选择

| 字段 | 含义 |
| --- | --- |
| `validation` | 开启训练期标准 R2R 评测；未显式开启时不加载 Val-Unseen |
| `validation-fast-subset-size=128` | 固定快评样本数；ID 由 seed+instr_id 的 SHA256 一次性选定 |
| `validation-fast-interval-steps=1000` | 每 1000 optimizer step 强制保存并快评 |
| `validation-max-new-tokens=512` | 每次导航 tool decision 最多生成 512 tokens |
| `validation-seed=0` | 固定 subset 身份与贪心评测协议 |

快评和完整评测都使用贪心解码，不采样。候选顺序按 `SPL`、`SR`、`nDTW`、
更低 `NE`、更早 step 依次比较。`save-total-limit=3` 只清理 Trainer checkpoint；
每个待评候选会先原子复制为 `validation/snapshots/step-N`，其中只有三份 LoRA
推理文件和逐文件哈希，不会随旧 checkpoint 删除。`validation/queue.json` 记录
评测事件与 job 状态，`validation/state.json` 只保存 selector 的 quick/full best。

`PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True` 用于降低动态生成长度造成的
显存碎片。`TOKENIZERS_PARALLELISM=false` 和 `OMP_NUM_THREADS=1` 用于避免四个
进程各自创建过多 CPU 线程。

## 6. 启动后立即检查

```bash
TRAIN_PID=$(cat /tmp/navgpt-main8.1-pid)
OUTPUT_DIR=$(cat /tmp/navgpt-main8.1-output-dir)
CONSOLE_LOG=$(cat /tmp/navgpt-main8.1-console-log)

ps -fp "$TRAIN_PID"

echo "OUTPUT_DIR=$OUTPUT_DIR"
echo "CONSOLE_LOG=$CONSOLE_LOG"

tail -n 100 "$CONSOLE_LOG"
```

确认日志包含：

```text
mode=ddp visible_gpus=0,1,2,3 nccl_profile=blackwell-safe
```

并且没有：

```text
Traceback
CUDA out of memory
illegal memory access
NCCL WARN
```

查看四卡状态：

```bash
watch -n 2 nvidia-smi
```

退出 `watch` 使用 `Ctrl+C`，这不会终止后台训练。

## 7. 训练过程中监控

持续查看控制台：

```bash
CONSOLE_LOG=$(cat /tmp/navgpt-main8.1-console-log)
tail -f "$CONSOLE_LOG"
```

退出 `tail -f` 使用 `Ctrl+C`，不会终止后台训练。

查看最新训练指标：

```bash
TRAIN_PYTHON=/home/xl/venvs/navgpt-rl/bin/python
OUTPUT_DIR=$(cat /tmp/navgpt-main8.1-output-dir)

test -f "$OUTPUT_DIR/logs/train_metrics.jsonl"

tail -n 1 "$OUTPUT_DIR/logs/train_metrics.jsonl" \
  | "$TRAIN_PYTHON" -m json.tool
```

查看已经收集多少条 rollout 和最近的结束原因：

```bash
OUTPUT_DIR=$(cat /tmp/navgpt-main8.1-output-dir)

wc -l "$OUTPUT_DIR/logs/navigation_rollouts.jsonl"
tail -n 4 "$OUTPUT_DIR/logs/navigation_rollouts.jsonl"
```

列出 checkpoint：

```bash
OUTPUT_DIR=$(cat /tmp/navgpt-main8.1-output-dir)

find "$OUTPUT_DIR" \
  -maxdepth 1 \
  -type d \
  -name 'checkpoint-*' \
  -print \
  | sort -V
```

查看验证进度和当前 best：

```bash
OUTPUT_DIR=$(cat /tmp/navgpt-main8.1-output-dir)

if test -f "$OUTPUT_DIR/validation/state.json"; then
  /home/xl/venvs/navgpt-rl/bin/python -m json.tool \
    "$OUTPUT_DIR/validation/state.json"
fi

if test -f "$OUTPUT_DIR/validation/queue.json"; then
  /home/xl/venvs/navgpt-rl/bin/python -m json.tool \
    "$OUTPUT_DIR/validation/queue.json"
fi

find "$OUTPUT_DIR/validation/snapshots" \
  -mindepth 1 -maxdepth 1 -type d -name 'step-*' \
  -print 2>/dev/null | sort -V

find "$OUTPUT_DIR/validation/evaluations" \
  -type f -name metrics.json -print 2>/dev/null | sort -V

find "$OUTPUT_DIR/validation/evaluations" \
  -type f -name 'predictions.rank-*.jsonl' \
  -exec wc -l {} + 2>/dev/null
```

验证期间训练会主动暂停，四张 GPU 各处理固定的四分之一，并在控制台输出
`R2R validation rank=... completed=...`。这不是训练卡死。若进程中断，使用
对应 checkpoint 续训时会先恢复 `queue.json` 中状态为 `queued/running` 的事件
和 job；snapshot 不会重建，已写入且通过校验的 episode 也不会重新生成。

每个完成的评测目录保留按 rank 可续写的 JSONL、`predictions.json`、
`per_item_metrics.json` 和聚合 `metrics.json`。标准指标由轨迹重新计算，不采用
环境奖励或训练日志里的 success 字段。

检查错误但不终止训练：

```bash
CONSOLE_LOG=$(cat /tmp/navgpt-main8.1-console-log)

grep -E \
  'Traceback|CUDA out of memory|illegal memory access|NCCL WARN|ChildFailedError' \
  "$CONSOLE_LOG" \
  | tail -n 100 || true
```

重点观察 `nav/success_rate`、`nav/episode_return_mean`、
`nav/final_distance_mean`、`nav/protocol_violation_rate`、三类
`nav/reward_family/*`、`kl`、`grad_norm` 和 `learning_rate`。不要只根据
`train_loss` 判断模型好坏。

## 8. 正常结束校验

训练完成后，launcher/torchrun 进程会退出，控制台末尾会打印最终 JSON。

```bash
TRAIN_PYTHON=/home/xl/venvs/navgpt-rl/bin/python
TRAIN_PID=$(cat /tmp/navgpt-main8.1-pid)
OUTPUT_DIR=$(cat /tmp/navgpt-main8.1-output-dir)
CONSOLE_LOG=$(cat /tmp/navgpt-main8.1-console-log)

if ps -p "$TRAIN_PID" >/dev/null 2>&1; then
  echo "training is still running"
else
  echo "training process has exited"
fi

tail -n 100 "$CONSOLE_LOG"

find "$OUTPUT_DIR" \
  -maxdepth 1 \
  -type d \
  -name 'final-adapter-step-*' \
  -print \
  | sort -V

tail -n 1 "$OUTPUT_DIR/logs/train_metrics.jsonl" \
  | "$TRAIN_PYTHON" -m json.tool
```

正常完成需要同时满足：

- 控制台没有 traceback/ChildFailedError；
- 最终 JSON 的 `global_step` 与预期一致；
- 存在 `final-adapter-step-N/adapter_model.safetensors`；
- 存在 `final-adapter-step-N/navgpt_adapter_manifest.json`；
- 最后一个 checkpoint 有 `optimizer.pt`、`scheduler.pt`、四份 RNG 和
  `navgpt_grpo_checkpoint.json`；
- `validation/queue.json` 中所有 event/job 的 `status` 都是 `completed`；
- `validation/state.json` 的 `full_best.adapter_path` 指向存在且带哈希清单的
  `validation/snapshots/step-N`；
- 至少一个 `validation/evaluations/full/step-*/metrics.json` 完整记录 2349 条。

## 9. 四卡断点续训命令

只在训练进程已经退出后执行。必须使用同一个输出目录、同样的 4 张卡数量、
同一个 `MAX_COMPLETION_LENGTH` 和完全一致的训练参数。

先确定最新 checkpoint：

```bash
set -e

cd /workspace/RL-NavGPT/nav_src

OUTPUT_DIR=$(cat /tmp/navgpt-main8.1-output-dir)
MAX_COMPLETION_LENGTH=$(cat /tmp/navgpt-main8.1-max-completion-length)

CHECKPOINT=$(
  find "$OUTPUT_DIR" \
    -maxdepth 1 \
    -type d \
    -name 'checkpoint-*' \
    -print \
  | sort -V \
  | tail -n 1
)

test -n "$CHECKPOINT"
test -d "$CHECKPOINT"

echo "OUTPUT_DIR=$OUTPUT_DIR"
echo "CHECKPOINT=$CHECKPOINT"
```

然后恢复：

```bash
TRAIN_PYTHON=/home/xl/venvs/navgpt-rl/bin/python
OUTPUT_DIR=$(cat /tmp/navgpt-main8.1-output-dir)
MAX_COMPLETION_LENGTH=$(cat /tmp/navgpt-main8.1-max-completion-length)

CHECKPOINT=$(
  find "$OUTPUT_DIR" \
    -maxdepth 1 \
    -type d \
    -name 'checkpoint-*' \
    -print \
  | sort -V \
  | tail -n 1
)

RESUME_LOG="${OUTPUT_DIR}.resume-$(date +%Y%m%d-%H%M%S).console.log"

test -d "$CHECKPOINT"
test ! -e "$RESUME_LOG"

nohup env \
  PYTHONNOUSERSITE=1 \
  PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True \
  TRL_EXPERIMENTAL_SILENCE=1 \
  TOKENIZERS_PARALLELISM=false \
  OMP_NUM_THREADS=1 \
  "$TRAIN_PYTHON" scripts/launch_grpo.py \
    --mode ddp \
    --gpus 0,1,2,3 \
    --nccl-profile blackwell-safe \
    -- \
    --output-dir "$OUTPUT_DIR" \
    --expected-instruction-count 14039 \
    --clip-text-device auto \
    --clip-text-dtype fp16 \
    --max-navigation-steps 10 \
    --r 16 \
    --lora-alpha 32 \
    --lora-dropout 0.05 \
    --max-trainable-percentage 1.0 \
    --dtype bf16 \
    --max-completion-length "$MAX_COMPLETION_LENGTH" \
    --num-generations 4 \
    --learning-rate 1e-6 \
    --weight-decay 0.0 \
    --warmup-ratio 0.03 \
    --max-grad-norm 1.0 \
    --lr-scheduler-type cosine \
    --beta 0.001 \
    --temperature 1.0 \
    --top-p 0.95 \
    --max-tool-calling-iterations 10 \
    --trainer-max-steps -1 \
    --num-train-epochs 2.0 \
    --logging-steps 1 \
    --save-steps 50 \
    --save-total-limit 3 \
    --trajectory-log-interval 10 \
    --validation \
    --validation-annotation ../datasets/R2R/annotations/R2R_val_unseen_instr.json \
    --validation-action-plan-cache ../datasets/R2R/action_plan_cache/qwen2.5-14b-val-unseen-t0-v1/R2R_val_unseen_action_plans.jsonl \
    --validation-fast-subset-manifest ../datasets/R2R/eval_subsets/R2R_val_unseen_fast128_seed0.json \
    --validation-expected-instruction-count 2349 \
    --validation-fast-subset-size 128 \
    --validation-fast-interval-steps 1000 \
    --validation-max-new-tokens 512 \
    --validation-seed 0 \
    --seed 0 \
    --resume-from-checkpoint "$CHECKPOINT" \
  > "$RESUME_LOG" 2>&1 &

TRAIN_PID=$!
printf '%s\n' "$TRAIN_PID" \
  > /tmp/navgpt-main8.1-pid
printf '%s\n' "$RESUME_LOG" \
  > /tmp/navgpt-main8.1-console-log

echo "TRAIN_PID=$TRAIN_PID"
echo "RESUME_LOG=$RESUME_LOG"
```

run manifest 会再次校验数据、模型、LoRA、优化参数、代码摘要和 world size。
任一不可变字段不一致都会拒绝续训，不要通过删除 manifest 绕过检查。

正常续训会自动先处理 `queue.json` 中未完成的评测，然后继续训练。如果只想先
完成评测、确认 best 状态而暂时不恢复 optimizer，在上述恢复命令的
`--resume-from-checkpoint "$CHECKPOINT"` 后再加一行 `--validation-only`。该命令
完成后会退出且不会产生新 checkpoint；确认队列全部 completed 后，再运行不带
`--validation-only` 的原恢复命令。

## 10. 单卡等价命令（不用于本次四卡主 run）

单卡算法流程与四卡相同，但四条 rollout 在 GPU 2 上串行生成，入口自动使用
`steps_per_generation=4` 和 `gradient_accumulation_steps=4`：

```bash
PYTHONNOUSERSITE=1 \
PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True \
TRL_EXPERIMENTAL_SILENCE=1 \
TOKENIZERS_PARALLELISM=false \
"$TRAIN_PYTHON" scripts/launch_grpo.py \
  --mode single \
  --gpus 2 \
  -- \
  --output-dir ../outputs/single-example \
  --max-completion-length "$MAX_COMPLETION_LENGTH" \
  --num-generations 4 \
  --trainer-max-steps 10
```

launcher 会自行设置 `CUDA_VISIBLE_DEVICES`，不要在外层重复设置。不要用单卡
checkpoint 恢复四卡 run，反之亦然；run manifest 会拒绝 world size 变化。

## 11. 哪些参数可以调整

任何参数变化都必须创建新输出目录，不能从旧 run checkpoint 继续。

可以作为后续独立实验调整：

- `seed`：多随机种子；
- `learning-rate`：建议单独尝试，不要在一个 run 中途改变；
- `beta`：KL 强度；
- `r/lora-alpha`：LoRA 容量消融；
- `num-train-epochs` 或 `trainer-max-steps`：训练长度；
- 奖励权重：当前不是 CLI 参数，需要以明确配置/代码版本形成独立消融 run。

当前不要随意调整：

- `max_completion_length`：必须来自对应 Level 2 报告；
- `num_generations/steps_per_generation/gradient_accumulation_steps`：当前 DDP
  契约固定一个完整 group；
- `max_navigation_steps` 与 `max_tool_calling_iterations`：两者需要同步设计；
- `nccl-profile`：当前服务器四卡必须保留 `blackwell-safe`；
- `dtype`：当前验证和显存预算基于 BF16。

## 12. 冻结加载训练后的 adapter

统一推理加载契约已经实现。它会校验 adapter manifest、基础模型权重指纹、
LoRA 超参数和 48 层目标覆盖，然后使用 `is_trainable=False` 加载 PEFT，确认
所有参数的 `requires_grad=False`、模型处于 `eval()`，并运行一次短生成：

```bash
cd /workspace/RL-NavGPT/nav_src

TRAIN_PYTHON=/home/xl/venvs/navgpt-rl/bin/python
POLICY_MODEL=../models/Qwen2.5-14B-Instruct-1M
ADAPTER_PATH=/workspace/RL-NavGPT/outputs/<run>/final-adapter-step-<N>

test -f "$ADAPTER_PATH/adapter_model.safetensors"
test -f "$ADAPTER_PATH/navgpt_adapter_manifest.json"

PYTHONNOUSERSITE=1 CUDA_VISIBLE_DEVICES=2 \
  "$TRAIN_PYTHON" scripts/validate_lora_policy.py inference \
    --model-path "$POLICY_MODEL" \
    --adapter-path "$ADAPTER_PATH" \
    --dtype bf16 \
    --max-new-tokens 8
```

最后必须输出：

```text
PASS frozen policy inference load and generation
```

`NavGPT.py` 使用 Ours 时显式增加：

```text
--llm_backend hf
--local_model_path ../models/Qwen2.5-14B-Instruct-1M
--local_adapter_path /workspace/RL-NavGPT/outputs/<run>/<adapter-or-checkpoint>
--hf_device_map single
```

并通过 `CUDA_VISIBLE_DEVICES=<单张卡>` 启动。不要把训练 checkpoint 当成完整
14B 模型；它只包含 LoRA，必须同时提供原始基础模型目录。

## 13. 训练完成后还不能直接宣称 8.1 完成

冻结 adapter 加载、标准 R2R 指标和训练期 best 选择已经完成。下一步是用同一
独立评测入口完成三套系统的最终主对比：

- 基础 Qwen + 零样本（对比组 2 左侧）；
- 同一基础 Qwen + `final-adapter-step-N`（Ours）；
- NavGPT GPT-3.5 零样本（对比组 1 左侧）；
- 相同 Val-Unseen/Test split、最大导航步数、输入观测和指标实现；
- 完整输出 SR、SPL、NE、OSR、nDTW、SDTW、CLS 和逐条轨迹。

训练期 `nav/success_rate` 只用于诊断，不能替代以上独立测试结果。
