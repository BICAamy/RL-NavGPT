# 基于强化学习（GRPO）优化本地 LLM 视觉导航的技术实现详细步骤

本文档基于之前确立的创新思路：**放弃闭源的 GPT-3.5/4 API，转而使用开源本地大模型（如 Qwen 或 Llama），通过挂载 LoRA 适配器，并在 NavGPT 物理仿真环境中进行在线交互式强化学习（推荐 GRPO 算法），使本地小模型在物理导航任务上超越零样本大模型。**

以下是技术跑通及实验部署的详细步骤。

---

## 阶段一：模型准备与本地推理打通（脱离 API 依赖）

**目标：** 让本地开源模型完全替代 `nav_src/agent.py` 中的 `OpenAI` 调用。

### 第一步：从 HuggingFace / ModelScope 下载本地开源模型
为了保证能够在消费级或实验室常用的 GPU（如单卡 3090/A100 或者 双卡 4090）上跑通 RL 训练，建议下载 **7B-8B 级别**的模型。
* **主力模型推荐：** `Qwen/Qwen2.5-7B-Instruct` 或 `meta-llama/Meta-Llama-3-8B-Instruct`
* **调试模型推荐：** 先下载 `Qwen/Qwen2.5-0.5B-Instruct` 或 `1.5B` 版本，用于在低算力下快速排查代码 Bug（尤其是 RL 框架的 Bug）。

### 第二步：改造 NavGPT 的 LLM 接入层
原版 NavGPT 是纯调 OpenAI API 的。需要修改 `nav_src/agent.py` 及其 `LLMs/` 目录：
1. **编写本地模型包装器**：使用 HuggingFace `transformers` 库加载下载好的本地模型。
2. **适配 Prompt 模板**：修改 `prompt/planner_prompt.py`。开源模型对指令格式（如 `<|im_start|>` 等 Chat Template）比较敏感，需将原版 Prompt 套进模型的专属聊天模板中。
3. **跑通零样本基线 (Baseline 1)**：在不挂载任何训练的情况下，先让本地下载的 7B 模型直接用 NavGPT 的测例跑一轮推理，记录下 SR（成功率）和 SPL。这是你论文的**最弱基线**。

---

## 阶段二：强化学习环境与奖励函数的构建

**目标：** 打造能向 TRL 库输送轨迹（State, Action, Reward, Next_State）的接口。

### 第三步：封装标准强化学习环境包装器（Gymnasium 风格）
原项目 `nav_src/env.py` 是针对批量推理设计的。强化学习训练往往需要 `step()` 接口返回奖励。需要写一个 Wrapper 类：
1. **状态（State）生成**：包装 `get_obs()` 输出的信息（当前场景文本总结、历史导航记录等打包成的完整 Prompt 字符串）。
2. **动作（Action）执行**：接收模型输出的 Viewpoint ID 或 `Finish!` 指令，调用底层仿真器 `env.step([action])` 执行位移。
3. **是否结束（Done）**：超出最大步数（例如 10 步）、模型输出结束指令、或触发撞墙错误，返回 `Done=True`。

### 第四步：实现高定制化奖励函数（Reward Function）
根据《优化+创新.md》的设计，在每次 `step()` 被调用时，精确计算以下 Reward 并相加：
* `dist = shortest_distance(current_node, target_node)` 用底层环境的导航图算出。
* **路径效率奖励 (+5)**：如果这一步的 `dist` 比上一步小，给予正奖励。
* **回头路惩罚 (-10)**：记录一个 `visited_nodes` 集合，如果新到达的节点在集合中，给予惩罚。
* **无效动作/死胡同惩罚 (-20 或 -50)**：如果 `action` 返回的是无改变或撞墙。
* **最终终点奖励 (+100 到 +300)**：如果 `Done=True` 且当前点距离目标点 `<3m`（成功）。

---

## 阶段三：强化学习微调代码的编写（LoRA + GRPO）

**目标：** 构建 RL 主循环，只训练 LoRA 参数。

### 第五步：设置 LoRA 适配器（PEFT）
1. 使用 HuggingFace 的 `PEFT` 库，在加载的主模型上注入 LoRA 适配器。
2. **配置示例**：`r=16`, `lora_alpha=32`，配置需要训练的 Target Modules（对于 Qwen/Llama，一般是 `q_proj`, `k_proj`, `v_proj`, `o_proj`, `gate_proj`, `up_proj`, `down_proj`）。
3. **冻结主干**：确保 LLM 原始参数 `requires_grad = False`，只有 LoRA 权重允许更新。这不仅能极大地省显存，还能保留大模型的通用常识。

### 第六步：基于 TRL 库集成 GRPO 算法
*DeepSeek 证明了 GRPO 算法不需要庞大且冗余的 Critic 网络，特别适用于通过固定规则（我们写的 Reward Function）计算奖励的任务。*
1. 引入 HuggingFace `trl` 库中的 `GRPOTrainer` 或自己手写一个简单的 RL 交互收集循环。
2. **收集轨迹（Rollout）**：每次训练从 R2R 训练集选一批指令起始点。把本地带 LoRA 的模型放进去，采样它的决策，直至 `Done=True`，收集完整的轨迹数据（Prompt、模型输出的 Response、每步得到的 Reward）。
3. **优势函数与优化**：计算每个 Action 对应的 Advantage（优势函数值），反向传播更新 LoRA 参数。

---

## 阶段四：训练与验证

**目标：** 开始实验跑指标，拿到写论文的核心突破点。

### 第七步：在线交互与迭代训练
1. 开始正式的 Training Loop。观察 Reward 的变化曲线（是不是随着 epoch 增加，Agent 获得的平均奖励越来越高？是不是撞墙和走回头路的频率显著下降了？）。
2. 由于具身 RL 很容易出现“奖励黑客（Reward Hacking）”，比如智能体原地发呆来避免犯错，需要根据训练中的 Loss/Reward 曲线调整奖励分数权重（比如加重对原地发呆的惩罚）。
3. **保存 Checkpoint**：每隔数百个 Episode，保存一次 LoRA 权重。

### 第八步：ICRA 论文的核心消融与对比实验
利用测试集（Test / Val-Unseen）跑最终数据，向审稿人证明这套方法的价值：
1. **对比实验（Main Results）**：
   * NavGPT (GPT-3.5 零样本) vs **Ours (7B本地 + RL LoRA)** —— *证明 RL 后的小模型抗衡/超越闭源大厂模型。*
   * 本地 7B 零样本 vs **Ours (7B本地 + RL LoRA)** —— *证明绝非虚张声势，强化学习带来了实打实的质变。*
2. **消融实验（Ablation Study）**：
   * 移除“回头路惩罚”看模型会不会变蠢；移除“路径效率奖励”看能不能缩短总步数。
3. **定性分析（Qualitative Results）**：
   * 挑选 1-2 个具体的长指令导航 Case 画成图。比如，零样本模型走到一半忘记指令走回头路，而经过 RL 训练的 Ours 模型干脆利落地完成了连续拆解目标。